## 01. 필요 패키지 설치
```bash
$ uv venv
$ source ./venv/bin/activate

$ uv pip install torch tensorboard emoji

$ uv pip install -U transformers trl datasets accelerate evaluate sentencepiece bitsandbytes protobuf==3.20.3

$ uv add torch tensorboard emoji transformers trl datasets accelerate evaluate sentencepiece bitsandbytes protobuf
```

## 02. Model  & DataSet Pull
- model pull 때와 달리, DataSet pull 때는 `hf.co:datasets` 뒤에 주소?를 이어붙여야 한다.
```bash
# model
$ git clone git@hf.co:google/gemma-3-270m-it

# dataset
git clone git@hf.co:datasets/kr15t3n/text2emoji
```

## 03. Device & Dtype 확인
```python
import torch
from transformers import AutoTokenizer, AutoModelForCausalLM

# 로컬 경로로 수정
gemma_model = "/root/huggingface-models/gemma-3-270m-it"
base_model = AutoModelForCausalLM.from_pretrained(gemma_model, device_map="auto", attn_implementation="eager", dtype=torch.bfloat16)
tokenizer = AutoTokenizer.from_pretrained(gemma_model)

print(f"Device: {base_model.device}")
print(f"DType: {base_model.dtype}")
```

```bash
$ python ./01_check_device.py
Device: cuda:0
DType: torch.bfloat16
```

![[Pasted image 20251220062801.png]]

## 04. 훈련 데이터셋 로드 & 포멧
```python
import emoji
from datasets import load_dataset


# load dataset
# Use the first 2000 samples for efficient training.
general_dataset_path = load_dataset("kr15t3n/text2emoji", encoding="utf-8", split="train")

# Clean dataset to only use examples where 'emoji' field contains only emoji characters
def is_only_emoji(sample):
	emoji_string = sample['emoji']
	if not emoji_string:
		return False
	return all(emoji.is_emoji(char) for char in emoji_string)
dataset = general_dataset_path.filter(is_only_emoji)

print(f"\nHere's the 10th example from the dataset: {dataset[10]}")

# format training dataset
def translate(sample):
	return {
		"messages": [
		{"role": "system", "content": "Translate this text to emoji: "},
		{"role": "user", "content": f"{sample['text']}"},
		{"role": "assistant", "content": f"{sample['emoji']}"}
	]
}

training_dataset = dataset.map(translate, remove_columns=dataset.features.keys())
training_dataset_splits = training_dataset.train_test_split(test_size=0.1, shuffle=True)

print("\nHere's the 40th example from the formatted training dataset:")
print(training_dataset[40])
```

```bash
$ python ./02_load_and_format_training_dataset.py
Filter: 100%|██████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████| 2529/2529 [00:00<00:00, 179172.91 examples/s]

Here's the 10th example from the dataset: {'text': 'A supernova just exploded', 'emoji': '💥✨'}
Map: 100%|██████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████| 1519/1519 [00:00<00:00, 10231.24 examples/s]

Here's the 40th example from the formatted training dataset:
{'messages': [{'content': 'Translate this text to emoji: ', 'role': 'system'}, {'content': 'Avoiding responsibilities', 'role': 'user'}, {'content': '🏃💨', 'role': 'assistant'}]}
```


## 05. 기본 모델 테스트
```python
import torch
import re
from transformers import AutoTokenizer, AutoModelForCausalLM, pipeline
from random import randint
import emoji
from datasets import load_dataset


# Load and prepare the dataset
print("Loading dataset...")
general_dataset_path = load_dataset("kr15t3n/text2emoji", encoding="utf-8", split="train")

# Clean dataset to only use examples where 'emoji' field contains only emoji characters
def is_only_emoji(sample):
	emoji_string = sample['emoji']
	if not emoji_string:
		return False
	return all(emoji.is_emoji(char) for char in emoji_string)
dataset = general_dataset_path.filter(is_only_emoji)

def translate(sample):
	return {
		"messages": [
			{"role": "system", "content": "Translate this text to emoji: "},
			{"role": "user", "content": f"{sample['text']}"},
			{"role": "assistant", "content": f"{sample['emoji']}"}
		]
	}

training_dataset = dataset.map(translate, remove_columns=dataset.features.keys())
training_dataset_splits = training_dataset.train_test_split(test_size=0.1, shuffle=True)

print(f"Dataset loaded: {len(training_dataset_splits['train'])} training samples, {len(training_dataset_splits['test'])} test samples")

# Load model and tokenizer
print("Loading model...")
gemma_model = "/root/huggingface-models/gemma-3-270m-it"
tokenizer = AutoTokenizer.from_pretrained(gemma_model)

  
# Create a transformers inference pipeline
pipe = pipeline("text-generation", model=gemma_model, tokenizer=tokenizer)

# Select a random sample from the test dataset
rand_idx = randint(0, len(training_dataset_splits["test"]) - 1)
test_sample = training_dataset_splits["test"][rand_idx]

# Handle messages
all_messages = test_sample['messages']
user_message_content = next((msg['content'].strip() for msg in all_messages if msg['role'] == 'user'), "Not Found")
dataset_emoji = next((msg['content'].strip() for msg in all_messages if msg['role'] == 'assistant'), "Not Found")

prompt_messages = [
	{"role": "system", "content": "Translate this text to emoji: "},
	{"role": "user", "content": user_message_content}
]

# Apply the chat template. This will format the messages correctly for the model.
prompt = tokenizer.apply_chat_template(prompt_messages, tokenize=False, add_generation_prompt=True)

# Generate the output
output = pipe(prompt, max_new_tokens=64)
model_output_only = output[0]['generated_text'][len(prompt):].strip()

print(f"\nDataset text: {user_message_content}")
print(f"\nDataset emoji: {dataset_emoji}")
print(f"\nModel generated output: {model_output_only}")
```

- 실행 결과
```bash
$ python ./03_basic_mode_test.py
Loading dataset...
Dataset loaded: 1367 training samples, 152 test samples
Loading model...
Device set to use cuda:0

Dataset text: Scared of ghosts

Dataset emoji: 👻🤫🚪👀

Model generated output: Here are a few emojis that express "scared of ghosts":

*   **Scared**
*   **Ghost**
*   **Fear**
*   **Panic**
*   **Shadow**
*   **Silence**
*   **Fear**
*   **Shadow**
*
```

```bash
$ python ./03_basic_mode_test.py
Loading dataset...
Dataset loaded: 1367 training samples, 152 test samples
Loading model...
Device set to use cuda:0

Dataset text: Oh my god

Dataset emoji: 😱🤯😮

Model generated output: 😊
```

![[Pasted image 20251220073834.png]]

출력 결과과 정확하지 않은 상태 => 파인 튜닝

## 06. Fine-Tuning the model
- Hugging Face TRL 활용

### 06.01. TRL (Transformer Reinforcement Learning)
- full stack library where we provide a set of tools to train transformer language models with methods like Supervised Fine-Tuning (SFT), Group Relative Policy Optimization (GRPO), Direct Preference Optimization (DPO), Reward Modeling, and more.
	- 트랜스포머 언어 모델을 훈련할 수 있도록 제공되는 풀 스택 라이브러리
	- 트랜스포머와 통합돼있다.

### 06.02. Transformer
- [Github | huggingface/transformers](https://github.com/huggingface/transformers/blob/main/i18n/README_ko.md)
	- Transformers는 텍스트, 비전, 오디오와 같은 다양한 분야에서 여러 과제를 수행하는 수천 개의 사전 학습된 모델을 제공한다.
	- Transformers는 이러한 사전학습 모델을 빠르게 다운로드해 특정 텍스트에 사용하고, 원하는 데이터로 fine-tuning해 커뮤니티나 허깅페이스에 공유할 수 있도록 API를 제공
	- 모델 구조를 정의하는 각 파이썬 모듈은 완전히 독립적이여서 연구 실험을 위해 손쉽게 수정 가능
	- Transformers는 가장 유명한 3개의 딥러닝 라이브러리를 지원합니다. 이들은 서로 완벽히 연동됩니다 — [Jax](https://jax.readthedocs.io/en/latest/), [PyTorch](https://pytorch.org/), [TensorFlow](https://www.tensorflow.org/). 간단하게 이 라이브러리 중 하나로 모델을 학습하고, 또 다른 라이브러리로 추론을 위해 모델을 불러올 수 있습니다.


### 06.03. Fine-Tuning
- 필요 패키지 설치
```bash
$ uv pip install peft trl bitsandbytes && uv add peft trl bitsandbytes
```

- 코드 (04_fine_tune_model_by_TRL_plot_training_results.py)





- 실행 (약 5~10분 소요)
![[Pasted image 20251220075714.png]]

### 06.04 학습과정 시각화 코드 통합
- 필요 패키지 설치
```bash
$ uv pip install matplotlib && uv add matplotlib
```

- 시각화 코드 부분
```python
import matplotlib.pyplot as plt

# Access the log history
log_history = trainer.state.log_history

# Extract training / validation loss
train_losses = [log["loss"] for log in log_history if "loss" in log]
epoch_train = [log["epoch"] for log in log_history if "loss" in log]
eval_losses = [log["eval_loss"] for log in log_history if "eval_loss" in log]
epoch_eval = [log["epoch"] for log in log_history if "eval_loss" in log]

# Plot the training loss
plt.plot(epoch_train, train_losses, label="Training Loss")
plt.plot(epoch_eval, eval_losses, label="Validation Loss")
plt.xlabel("Epoch")
plt.ylabel("Loss")
plt.title("Training and Validation Loss per Epoch")
plt.legend()
plt.grid(True)
plt.show()
```

- 실행 결과
![[Pasted image 20251220082015.png]]
![[Pasted image 20251220082159.png]]
- 완료 
![[Pasted image 20251220083137.png]]

## 07. Merge Adapter
- 위의 과정을 통해서 원본 모델에 파인튜닝한 결과를 담은 파일을 어댑터라고 한다.
- 어댑터를 원본 모델과 병합해서, 파인 튜닝된 새로운 독립적 모델을 만드는 과정이다.
	- 원본 모델, 어댑터 파일은 기존 경로에 유지된다.
	- 머지 모델은 `merged_model_path` 에 저장된다.

```python
import torch
from transformers import AutoTokenizer, AutoModelForCausalLM
from peft import PeftModel

gemma_model = "/root/huggingface-models/gemma-3-270m-it"

# Choose which adapters to merge, otherwise defaults to latest
adapter_path = "/root/emoji-generator/myemoji-gemma-adapters/"

# Location of merged model directory
merged_model_path = "/root/emoji-generator/myemoji-gemma-merged/"

# Load base model and tokenizer
base_model = AutoModelForCausalLM.from_pretrained(gemma_model, device_map="auto")
tokenizer = AutoTokenizer.from_pretrained(adapter_path)

# Load and merge the PEFT adapters onto the base model
model = PeftModel.from_pretrained(base_model, adapter_path)
model = model.merge_and_unload()

# Save the merged model and its tokenizer
model.save_pretrained(merged_model_path)
tokenizer.save_pretrained(merged_model_path)

print(f"Model merged and saved to {merged_model_path}. Final model vocabulary size: {model.config.vocab_size}")
```

- 실행 결과
```bash
$ python ./06_merge_adapters.py

/root/emoji-generator/.venv/lib/python3.12/site-packages/peft/tuners/tuners_utils.py:1222: UserWarning: Model has `tie_word_embeddings=True` and a tied layer is part of the adapter, but `ensure_weight_tying` is not set to True. This can lead to complications, for example when merging the adapter or converting your model to formats other than safetensors. Check the discussion here: https://github.com/huggingface/peft/issues/2777
  warnings.warn(msg)
Model merged and saved to /root/emoji-generator/myemoji-gemma-merged/. Final model vocabulary size: 262144
```

![[Pasted image 20251220083235.png]]

## 08. Test merged model
### 08.01. 테스트 코드 (예시 활용)
- 코드 : 07.01_test_merged_model_.py
- 실행 결과
```bash
$ python ./07.01_test_merged_model_.py
Loading dataset...
Dataset loaded: 1367 training samples, 152 test samples
Loading model...
The tokenizer you are loading from '/root/emoji-generator/myemoji-gemma-merged/' with an incorrect regex pattern: https://huggingface.co/mistralai/Mistral-Small-3.1-24B-Instruct-2503/discussions/84#69121093e8b480e709447d5e. This will lead to incorrect tokenization. You should set the `fix_mistral_regex=True` flag when loading this tokenizer to fix this issue.
The module name  (originally ) is not a valid Python identifier. Please rename the original module to avoid import issues.
Device set to use cuda:0

Dataset text: He's shooting his shot

Dataset emoji: 🏀😉

Model generated output: 🎯🎯🎯
```

![[Pasted image 20251220084517.png]]

### 08.02. 테스트 코드(대화형)
- 코드 : 07_test_merged_model.py 
- 실행 결과
```bash
$ python ./07_test_merged_model.py 

Loading dataset...
Dataset loaded: 1367 training samples, 152 test samples
Loading fine-tuned merged model...
The tokenizer you are loading from '/root/emoji-generator/myemoji-gemma-merged/' with an incorrect regex pattern: https://huggingface.co/mistralai/Mistral-Small-3.1-24B-Instruct-2503/discussions/84#69121093e8b480e709447d5e. This will lead to incorrect tokenization. You should set the `fix_mistral_regex=True` flag when loading this tokenizer to fix this issue.
The module name  (originally ) is not a valid Python identifier. Please rename the original module to avoid import issues.
Device set to use cuda:0
Model loaded successfully!

======================================================================
Testing fine-tuned model with random samples from test dataset:
======================================================================

Sample 1:
  Input text: I have mixed drinks about feelings.
  Expected emoji: 🍹🍸🥃🤔😂
  Model output: 🍹🍸🥂🍾

Sample 2:
  Input text: What was that loud noise
  Expected emoji: 🤨💥
  Model output: 🙉🙉🙉

Sample 3:
  Input text: I love watching documentaries about space.
  Expected emoji: 🚀🪐🔭
  Model output: 🔭🪐✨

Sample 4:
  Input text: Hold on let me check that
  Expected emoji: ✋🤔💻
  Model output: ❓🤔🧐

Sample 5:
  Input text: My crypto investment went bust
  Expected emoji: 📉💸💀
  Model output: 📉💸📉

======================================================================
Interactive mode - Type your text (or 'quit' to exit):
======================================================================

Your text: hello
  Emoji: 👋🙂

Your text:  good morning
  Emoji: ☀️👌✨

Your text: quit
Goodbye!
```
### 08.03 테스트 코드 + 스크립트
- 코드 : 08_quick_test.py
- 실행 결과
```bash
$ python 08_quick_test.py 

Loading fine-tuned model...
The tokenizer you are loading from '/root/emoji-generator/myemoji-gemma-merged/' with an incorrect regex pattern: https://huggingface.co/mistralai/Mistral-Small-3.1-24B-Instruct-2503/discussions/84#69121093e8b480e709447d5e. This will lead to incorrect tokenization. You should set the `fix_mistral_regex=True` flag when loading this tokenizer to fix this issue.
The module name  (originally ) is not a valid Python identifier. Please rename the original module to avoid import issues.
Device set to use cuda:0
Model loaded!

============================================================
Quick Test: Text to Emoji Translation
============================================================
I love you                → 😘💖🥰😊
Happy birthday            → 🎂🥳🎁
Good morning              → ☀️☕🥐
I'm so angry              → 😠😡😤😤
Pizza time                → 🍕😋🍕
Going to the gym          → 💪🏋️🤸💪
Studying for exams        → 📚🤓📝
Beautiful sunset          → 🌅🌇🧡💜
Coffee break              → ☕😌🧘
Weekend party             → 🎉🥳🍾🥂
============================================================

Test completed!
```

![[Pasted image 20251220084339.png]]



---

# References
- 